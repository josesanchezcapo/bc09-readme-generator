function readmeGenerator(myResponses) {

    const readmeFormat = `
    
    # ${myResponses.projectTitle}
    
    `

    // return readme format
    return readmeFormat;
    

}

module.exports = readmeGenerator;